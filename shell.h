#ifndef _SHELL_H_
#define _SHELL_H_

#include <unistd.h>
#include <stdlib.h>
#include <stdio.h>
#include <sys/wait.h>
#include <string.h>
#include "shell.h"

int main(void)
char *getenv(const char *getenv);
ssize_t input_buff(ino_t *info, char **buff, size_t *len)

#endif
